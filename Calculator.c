#include <stdio.h>

int main() {

        int a;

                printf("Hey, pick a number, and follow the steps: ");
                scanf("%d", &a);

        int b;
                printf("Pick another one: \n");
                scanf("%d", &b);

        int c;
                printf("Select the signal that you whana use:   ( - or +) \n");

        int d;

                if (d == -) {
                printf("The results are %d \n", a + b);
        }

                else {
                printf("The results are %d", a + b);
        }
}


